class StaticPagesController < ApplicationController
  def index
    # we don't have any logic  here yet so
    # we are going to render the page 'index.html'
    # if we change the method name 'index' change the page name too  
  end
  def dog
    #create a new method
  end
end
